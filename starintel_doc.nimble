# Package

version     = "0.1.2"
author      = "nsaspy"
description = "Parse and handle starintel docs"
license     = "MIT"

# Deps

requires "nim >= 1.6.0"
requires "libSHA >= 1.0"
requires "urand >= 0.1.0"
