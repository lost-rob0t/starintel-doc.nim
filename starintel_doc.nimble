# Package

version     = "0.1.4"
author      = "nsaspy"
description = "Parse and handle starintel docs"
license     = "MIT"
srcDir       = "src"
# Deps

requires "nim >= 1.6.0"
requires "libSHA >= 1.0"
requires "urand >= 0.1.0"
